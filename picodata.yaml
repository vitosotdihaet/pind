cluster:
  name: "pind"
  tier:
    default:
      can_vote: true
    pind:
      replication_factor: 1
  default_replication_factor: 2
  default_bucket_count: 10

instance:
  log:
    level: warn
    format: plain
  memtx:
    memory: 67108864

